#include "main.h"
/**
 *_isdigit - checks for a digit (0 through 9)
 *@c: value to be computed
 *Return: 1 if digit otherwise 0.
 */
int _isdigit(int c)
{
	if (n >= 48 && n <= 57)
		return (1);
	else
		return (0);
	_putchar("\n");
}
